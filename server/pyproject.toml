[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Dmitriy", email = "d.a.muravyev1@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pyserial>=3.5",
    "requests>=2.34.2",
]

[project.scripts]
server = "server:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
